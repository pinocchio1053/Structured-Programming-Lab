#include<stdio.h>
int main()
{
   float length,width,area;

    printf("Enter the Length=",length);
    scanf("%f",&length);

    printf("Enter the Width=",width);
    scanf("%f",&width);

    area= (float) length*width;

        printf("Area of Ractangle is = %.3f",area);

    return 0;
}
